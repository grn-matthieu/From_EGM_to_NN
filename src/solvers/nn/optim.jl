"""
NNOptim (REMOVED)

This file used to contain a small local optimizer implementation. It was
removed in favor of the well-maintained `Optimisers.jl` package. Importing
this module now produces a clear error to avoid accidental usage.
"""
module NNOptim

error("NNOptim has been removed from the codebase. Use Optimisers.jl instead.")

end # module
